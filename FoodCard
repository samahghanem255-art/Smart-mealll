import { Link } from "react-router-dom";

export default function FoodCard({ food }) {
  return (
    <div className="card">
      <img src={food.image} alt={food.name} />
      <h3>{food.name}</h3>
      <p>{food.price} DA</p>

      <Link to={`/details/${food.id}`} className="btn">
        Voir
      </Link>
    </div>
  );
}
